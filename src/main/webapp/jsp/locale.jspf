<!DOCTYPE html>
<html>
    <form action="${pageContext.request.contextPath}/controller" method="post">
        <div class="locale">
            <button type="submit" name="lang" value="uk">UA</button>
            <button type="submit" name="lang" value="en">EN</button>
            <input name="command" value="change_locale" hidden="hidden" >
            ${sessionScope.locale}
        </div>
    </form>
    <fmt:setLocale value="${locale}" scope="session"/>
    <fmt:setBundle basename="resources"/>
    <c:set var="current_locale" value="${locale}" scope="session"/>
</html>